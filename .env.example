# R2 Upload Configuration
# These credentials are only needed for uploading files to R2 using the upload script
# They are NOT needed for runtime - the app uses R2 bindings from wrangler.json

CLOUDFLARE_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
