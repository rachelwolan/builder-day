/** @type {import('next').NextConfig} */
const nextConfig = {
  // Webflow Cloud compatibility
  output: 'standalone',
  // Enable React strict mode
  reactStrictMode: true,
}

module.exports = nextConfig

